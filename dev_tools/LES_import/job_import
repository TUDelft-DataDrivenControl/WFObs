#PBS -l nodes=1:ppn=1 				
#PBS -m ae 							
#PBS -M B.M.Doekemeijer@tudelft.nl 

# Activate the Matlab version I want e.g., 'module load matlab/2012b'
module load 'matlab/2016b'

# Run my M file and don't even try to display graphics
cd $HOME/WFObs/dev_tools/LES_import
matlab -nodisplay -noFigureWindows -logfile 'output.log' -r LES_import